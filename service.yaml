apiVersion: v1
kind: Service
metadata:
  name: production-web-service
spec:
  selector:
    app: web-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: NodePort   # ट्रॅफिक बाहेरून एक्सेस करण्यासाठी नोडपोर्ट
